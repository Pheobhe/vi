{% extends 'base.html.twig' %}

{% block title %}Organismo{% endblock %}

{% block body %}
    <h1>Organismo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organismo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ organismo.Nombre }}</td>
            </tr>
            <tr>
                <th>Domicilio</th>
                <td>{{ organismo.Domicilio }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ organismo.Telefono }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_organismo_index') }}">back to list</a>

    <a href="{{ path('app_organismo_edit', {'id': organismo.id}) }}">edit</a>

    {{ include('organismo/_delete_form.html.twig') }}
{% endblock %}
